import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'discord-permission-manager',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
